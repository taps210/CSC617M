// Expected: Semantic error (return type mismatch: expected int but got bool)

agent A {
    int x, y;
    update { move(self.x, self.y); }
}

world W {
    bool spawned = false;
    pre {
        if (!spawned) {
            spawn A(0, 0);
            spawned = true;
        }
    }
    post {}
}

int f() {
    return true; // wrong return type
}

void main() {
    int x;
    x = f();
    step();
}
