// Expected: Semantic error ('self' only valid inside an agent body)

agent A {
    int x, y;
    update { move(self.x, self.y); }
}

world W {
    bool spawned = false;
    pre {
        if (!spawned) {
            spawn A(0, 0);
            spawned = true;
        }
    }
    post {}
}

void main() {
    print(self.x); // invalid: self used in main
    step();
}
