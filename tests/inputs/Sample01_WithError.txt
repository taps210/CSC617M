// Types and I/O: simple ABM with primitives and read/print
agent Logger {
    int count;
    float value;
    string name;
    bool active;
    char tag;

    update {
        count = count + 1;
        value = value + 0.5;
        if (active) {
            move(0, 0);
        }
    }
}

world Env {
    int size = 10;

    update {
        // minimal env
    }
}

void main() {
@
    spawn Logger(0, 0.0, "agent", true, 'L') for 3;
    step();
    step();
    int n = 42;
    float f = 3.14;
    string s = "hello";
    bool b = true;
    read(n);
    print(s);
    print(n);
}
